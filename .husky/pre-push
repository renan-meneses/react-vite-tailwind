#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running lint..."
yarn lint || exit 1

echo "🏗 Running build..."
yarn build || exit 1

if yarn test --silent 2>/dev/null; then
  echo "🧪 Running tests..."
  yarn test || exit 1
fi

echo "✅ Pre-push validation passed"